Add settings to manage block interactions with other blocks (like canTeleport...etc.)
sprite connection with other blocks
map loader
Add switches that must be activate with a block (weight switches)
use <React.Fragment> in place of useless divs
smooth controls
Remove timeout for switch when doors are open
Manage multiples teleporter with same 'id'

/* corrections */
Shoot at start position

/* bonus */
Create redirect which direction depends on player direction + self direction
Create block which position depends on player position (or reversed)