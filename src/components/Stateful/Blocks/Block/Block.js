import { connect } from 'react-redux';

import Block from '../../../Stateless/Blocks/Block/Block';

const mapStateToProps = () => ({});

export default connect(mapStateToProps, null, null, {withRef: true})(Block);
